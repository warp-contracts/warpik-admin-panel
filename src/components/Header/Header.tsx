import type { Component } from 'solid-js';
import { Box } from '@suid/material';

const Header: Component = () => {
  return <Box sx={{ fontSize: '40px', paddingBottom: '50px', fontWeight: 600 }}>Warpik Admin Panel</Box>;
};

export default Header;
